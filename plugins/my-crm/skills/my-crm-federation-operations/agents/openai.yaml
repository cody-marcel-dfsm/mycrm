interface:
  display_name: "My CRM Federation"
  short_description: "Compare CRM sources with provenance"
  default_prompt: "Use $my-crm-federation-operations to compare this customer across sources."
