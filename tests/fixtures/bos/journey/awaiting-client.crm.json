{
  "identity": "crm-attendee-evidence",
  "status": "awaiting_client",
  "current_step": {
    "code": "resolve_crm_evidence",
    "type": "client"
  },
  "instruction": {
    "goal": "Find current CRM evidence for the meeting attendee.",
    "message": "Resolve the attendee against the current CRM sources.",
    "lookup_text": "fixture.attendee@example.invalid",
    "student_id": "student-public-42",
    "after_success": {
      "verb": "complete",
      "method": "POST",
      "href": "/actions/complete?capability=cap_public_complete",
      "payload_schema": {
        "type": "object",
        "additionalProperties": false,
        "required": ["acknowledged"],
        "properties": {
          "acknowledged": {
            "const": true
          }
        }
      }
    },
    "on_failure": {
      "verb": "failed",
      "method": "POST",
      "href": "/actions/failed?capability=cap_public_failed",
      "payload_schema": {
        "type": "object",
        "additionalProperties": false,
        "required": ["code", "message"],
        "properties": {
          "code": {
            "type": "string"
          },
          "message": {
            "type": "string"
          }
        }
      }
    }
  }
}
